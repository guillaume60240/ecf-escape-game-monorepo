import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "Game Master Manager",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
